

function Statcard({title , value}){
    return (
        
        <div className="stat-card">
            <h1>{title}</h1>
            <p>{value}</p>
        </div>
        
    )
}

export default Statcard;